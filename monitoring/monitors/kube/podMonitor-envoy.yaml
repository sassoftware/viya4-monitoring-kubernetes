apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: envoy
spec:
  selector:
    matchLabels:
      app.kubernetes.io/component: envoy
  namespaceSelector:
    any: true
  podMetricsEndpoints:
  - port: metrics
    path: /stats/prometheus
