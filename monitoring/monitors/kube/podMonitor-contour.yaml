apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: contour
spec:
  selector:
    matchLabels:
      app.kubernetes.io/component: contour
  namespaceSelector:
    matchNames:
    - projectcontour
  podMetricsEndpoints:
  - port: metrics
    path: /metrics
