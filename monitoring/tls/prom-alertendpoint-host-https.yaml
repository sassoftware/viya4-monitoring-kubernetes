prometheus:
  prometheusSpec:
    alertingEndpoints:
      - name: v4m-alertmanager 
        namespace: monitoring
        port: http-web
        scheme: https
        tlsConfig:
          insecureSkipVerify: true
