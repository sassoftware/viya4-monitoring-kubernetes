These are reference copies of the CI/CD workflows from the plugin's original repo (`summer_2026_grafana_chatbot`, owned by `Joel-Kenline_sasinst`) at the time it was vendored in here. They are **inert** — not under `.github/workflows/`, so nothing here runs automatically. They're kept only to document how the upstream repo builds/tests/releases the plugin (including the official Grafana plugin build/release action in `release.yml`), in case this repo's own CI is ever extended to build the plugin directly.

Do not move these into `viya4-monitoring-kubernetes/.github/workflows/` without adapting them first — `release.yml` in particular targets the Grafana plugin catalog release process for the original repo and would need real changes (repo references, secrets, triggers) before running here.
